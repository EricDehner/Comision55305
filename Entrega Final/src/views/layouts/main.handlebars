<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <title>Entrega Final</title>
    {{!-- Google Fonts --}}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=M+PLUS+Rounded+1c&family=Roboto&display=swap" rel="stylesheet">
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    {{!-- Toastify --}}
    <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css">
    {{!-- Toastify --}}
    <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css">
</head>

<body class="body">
    <header class="header">
        <nav class="navBar">
            <h1 class="navBar_brand">E-Commerce</h1>
            <div class="navBar_content">
                {{#if user}}

                {{#if (dif user.role "admin")}}
                <div class="navBar_content-dropdown">
                    <button onclick="dropdown()" class="navBar_content-item--btn">PRODUCTOS <span
                            class="material-symbols-outlined rotate-icon"> expand_more </span></button>
                    <div id="dropdown-content" class="navBar_content-item--dropdown">
                        <a class="navBar_content-dropdown--item" href="/products">Todos los productos</a>
                        <a class="navBar_content-dropdown--item" href="/products?query=bebidas">Bebidas</a>
                        <a class="navBar_content-dropdown--item" href="/products?query=congelados">Congelados</a>
                        <a class="navBar_content-dropdown--item" href="/products?query=caldos">Caldos</a>
                        <a class="navBar_content-dropdown--item" href="/products?query=limpieza">Limpieza</a>
                    </div>
                </div>
                {{else}}
                <a class="navBar_content-item" href="/realtimeproducts">PRODUCTOS</a>
                <a class="navBar_content-item" href="/purchases">COMPPRAS</a>
                {{/if}}
                {{#if (dif user.role "admin")}}
                <a class="navBar_content-item" href="/messages">CONTACTO</a>
                {{else}}
                <a class="navBar_content-item" href="/messages">MENSAJES</a>
                {{/if}}
                {{#if (dif user.role "admin")}}
                {{#if user.cart}}
                <p id="cartQty"></p>
                {{else}}
                {{/if}}
                <a class="navBar_content-item" href="/cart/{{user.cart}}"><span
                        class="material-symbols-outlined cart_icon">
                        shopping_cart
                    </span></a>
                {{else}}
                <a class="navBar_content-item" href="/adminController">USUARIOS</a>
                {{/if}}
                {{#if user}}
                {{#if (eq user.role "admin")}}
                <a class="navBar_content-item profile userName" href="/profile" data-tooltip="Admin"><span
                        class="material-symbols-outlined user">
                        key </span></a>
                {{else}}
                <a class="navBar_content-item profile userName" href="/profile"
                    data-tooltip="{{user.first_name}} {{user.last_name}}"><span class="material-symbols-outlined user">
                        person </span></a>
                {{/if}}
                {{else}}
                <a class="navBar_content-item profile" href="/profile" data-tooltip="Admin"><span
                        class="material-symbols-outlined user">
                        person </span></a>
                {{/if}}
                {{else}}
                {{/if}}
            </div>
        </nav>
    </header>
    <main class="main">
        {{{body}}}
    </main>
    <footer class="footer">
        <p class="footer_text">Proyecto BackEnd</p>
    </footer>
    <script src="/js/main.js"></script>
    <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
</body>

</html>